# File name
--output %(title)s.%(ext)s

# Download the best video with the best extension
# (For video, mp4 > webm > flv. For audio, m4a > aac > mp3 ...)
--format-sort "ext"

# Embed stuff into output file
--embed-metadata
--embed-chapters
--embed-subs
--embed-thumbnail
# and also into extended file attributes
--xattrs

